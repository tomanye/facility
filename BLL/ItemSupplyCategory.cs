
// Generated by MyGeneration Version # (1.3.0.3)

using System;
using DAL;

namespace BLL
{
	public class ItemSupplyCategory : _ItemSupplyCategory
	{
		public ItemSupplyCategory()
		{
		
		}

        public bool CategoryExists(int itemId, int CategoryId)
        {
            bool value = false;

            this.FlushData();
            this.Where.WhereClauseReset();
            this.Where.ItemID.Value = itemId;
            this.Where.CategoryID.Conjuction = MyGeneration.dOOdads.WhereParameter.Conj.And;
            this.Where.CategoryID.Value = CategoryId;
            this.Query.Load();
            value = (this.DataTable.Rows.Count > 0) ? true : false;

            return value;
        }
    }
}
