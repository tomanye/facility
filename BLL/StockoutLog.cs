
// Generated by MyGeneration Version # (1.2.0.7)

using System;
using DAL;
using System.Data;
using System.Collections.Specialized;

namespace BLL
{
	public class StockoutLog : _StockoutLog
	{
		public StockoutLog()
		{
		
		}

        public DataTable LoadByStoreAndItemId(int itemId, int storeId, bool openOnly)
        {
            string query = string.Empty;
            if (openOnly)
            {
                query = String.Format(@"Select * from StockoutLog where ItemID = {0} and StoreId ={1} and EndDate is null", itemId, storeId);
            }
            else
            {
                query = String.Format(@"Select * from StockoutLog where ItemID = {0} and StoreId ={1} and EndDate is not null", itemId, storeId);
            }

            this.LoadFromRawSql(query);
            return this.DataTable;
        }
	}
}
